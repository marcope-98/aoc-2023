#ifndef AOC_UTILS_ALIASES_HPP_
#define AOC_UTILS_ALIASES_HPP_

#include <cstdint>
#include <string>
#include <vector>

namespace aoc
{
  using vstring = std::vector<std::string>;
  using Matrix  = std::vector<std::vector<std::size_t>>;
} // namespace aoc

#endif